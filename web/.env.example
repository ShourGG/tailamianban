# 🔧 环境配置模板文件
# 📋 复制此文件为 .env 并根据实际情况修改配置

# ═══════════════ 应用配置 ═══════════════

# 应用标题
VITE_APP_TITLE=泰拉瑞亚管理面板

# API 基础路径
# 开发环境: http://localhost:8080/api
# 生产环境: /api (使用相对路径)
VITE_API_BASE_URL=/api

# 公共路径 (部署在子目录时需要修改)
VITE_PUBLIC_PATH=/

# 当前环境 (development | production | staging | test)
VITE_ENV=production

# 路由模式 (history | hash)
# history: 使用 HTML5 History API (推荐，需要服务器配置)
# hash: 使用 URL hash (兼容性好，无需服务器配置)
VITE_ROUTER_MODE=history

# ═══════════════ 开发配置 (可选) ═══════════════

# 开发服务器端口 (仅开发环境)
# VITE_DEV_PORT=5173

# 开发服务器主机 (仅开发环境)
# VITE_DEV_HOST=0.0.0.0

# 是否启用 HTTPS (仅开发环境)
# VITE_DEV_HTTPS=false

# ═══════════════ 其他配置 (可选) ═══════════════

# WebSocket 连接地址 (留空则使用当前域名)
# VITE_WS_URL=ws://localhost:8080/ws

# 是否启用 Mock 数据 (仅开发环境)
# VITE_USE_MOCK=false

# ═══════════════ 注意事项 ═══════════════
# 1. 所有环境变量必须以 VITE_ 开头才能在前端代码中访问
# 2. 修改环境变量后需要重启开发服务器
# 3. .env 文件不应提交到版本控制系统
# 4. 敏感信息（如密钥）请使用服务器环境变量